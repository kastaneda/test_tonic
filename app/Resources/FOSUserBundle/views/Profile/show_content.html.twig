
<dl class="dl-horizontal">
    <dt>{{ 'profile.show.username'|trans({}, 'FOSUserBundle') }}</dt>
    <dd>{{ user.username }}</dd>

    <dt>{{ 'profile.show.email'|trans({}, 'FOSUserBundle') }}</dt>
    <dd>{{ user.email }}</dd>

    <dt>{{ 'profile.show.full_name'|trans({}, 'UserBundle') }}</dt>
    <dd>{{ user.nameFirst }} {{ user.nameLast }}</dd>

    <dt>{{ 'profile.show.ref_code'|trans({}, 'UserBundle') }}</dt>
    <dd>{{ user.refCode }}</dd>
</dl>

<p>
    <a href="{{ path('fos_user_profile_edit') }}" class="btn btn-default">{{ 'profile.btn.edit'|trans({}, 'UserBundle') }}</a>
    <a href="{{ path('fos_user_change_password') }}" class="btn btn-default">{{ 'profile.btn.password'|trans({}, 'UserBundle') }}</a>
</p>
